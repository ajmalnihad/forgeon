# services/ — cross-domain business logic layer (Stage 2+).
#
# Planned modules (DO NOT implement before their stage):
#   sale_service.py      — sale create/edit inside transactions, totals
#   payment_service.py   — mark-paid transition (original sale_date preserved)
#   loyalty_service.py   — distinct-date loyalty count + preview
#   trash_service.py     — soft delete / restore
#   report_service.py    — report aggregations
#
# Stage 1 intentionally contains no business services.
